const y = ['желтый', 'зеленый', 'красный', 'синий', 'оранжевый'];
const z = ['механическая', 'автоматическая'];
const w = ['есть', 'нет'];
const v = ['ткань', 'кожа', 'комбиированая'];

conf = {
  arr: [
    num = 1, 
    colorAuto = y,
    korobka = z,
    condition = w,
    salon = v
   ]
   
}

console.log(conf)
